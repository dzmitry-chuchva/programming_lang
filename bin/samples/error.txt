// file: error.txt
// descr: error messages testing


int a, b;

float undef_func();		// неописанная функция
int func_with_args(int a, float b);

int entry()
{
        int a1 = 1, b1 = 2; float c = -10,d = 5.0;
		int c;		// повторное описание

        b1 = d;		// неявное приведение типов
        a1 = b1 % d;		// операция работает только с целыми числами

        undef_var = c + d;		// использование необъявленной переменной

        func_with_args(a);		// количество 											// переданных параметров не совпадает с 							// объявленным
		func_with_args = 5;		// функция в качестве L-значения
}

int func_with_args(int a, float b, int new)	// количество параметров в 									// определении отличается от их									// количества в объявлении
{
        return a + b;				// неявное приведение типов
}

